/**
 * DashboardShell — backward-compatible re-export of AppShell.
 * All new code should import { AppShell } from "@/components/shared/AppShell".
 * This file exists so existing page imports keep working without a mass rename.
 */
export { AppShell as DashboardShell } from "@/components/shared/AppShell";
